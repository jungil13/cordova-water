# Nixpacks config for Railway/Render PHP deployment
[phases.setup]
nixPkgs = ["php", "phpExtensions.pdo", "phpExtensions.pdo_mysql", "phpExtensions.mbstring", "phpExtensions.curl"]

[phases.install]
cmds = []

[start]
cmd = "php -S 0.0.0.0:${PORT:-8080} -t ."
